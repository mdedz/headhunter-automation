# HeadHunter Automation (WIP)

**HeadHunter Automation** — это инструмент автоматизации действий на платформе HeadHunter.
Проект находится **в активной разработке**, поэтому интерфейс, функциональность и структура кода могут изменяться.

---

## 🚧 Статус проекта

Проект пока **не завершён**.
Некоторые функции могут быть недоступны или работать нестабильно.

---

## 📦 Требования

* Arch Linux (или любой другой дистрибутив на основе Arch)
* Python **3.10+**
* Poetry (менеджер зависимостей)

---

## 🔧 Установка Poetry

Если Poetry ещё не установлен:

```bash
sudo pacman -S python-poetry
```

Проверить версию:

```bash
poetry --version
```

---

## 📥 Установка зависимостей

1. Клонируем репозиторий:

```bash
git clone https://github.com/mdedz/headhunter-automation
cd headhunter-automation
```

2. Устанавливаем зависимости:

```bash
poetry install
poetry install --extras "qt"
```

---

## ▶️ Запуск проекта

Запуск через poetry с AI сопроводительными письмами  :

```bash
poetry run headhunter-automation apply-similar --force-message --use-ai
```

---
