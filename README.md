<!-- omit from toc -->
# **AI Headhunter Automation – Поиск работы на hh.ru с помощью ИИ**

Инструмент для автоматизации откликов, переписки с работодателями, обновления резюме и работы с API HeadHunter — с использованием **искусственного интеллекта**.

**AI Headhunter Automation** — это CLI-инструмент нового поколения, созданный специально для соискателей, которые хотят быстрее получать ответы и повышать свои шансы на трудоустройство.

В отличие от обычных “скриптов для откликов”, инструмент использует **AI-алгоритмы**, **LLM-модели**, интеллектуальный фильтр релевантности и генерацию текстов, делая поиск работы умным и эффективным.

Проект превращает рутинные процессы — отклики, ответы, обновления резюме, уборку откликов — в полноценную **AI-стратегию поиска работы**, которая работает за вас.

---

<!-- omit from toc -->
## ✨ **Что делает этот проект особенным?**

### 🔥 **ИИ на каждом этапе**

Ваш ассистент:
* анализирует вакансии на релевантность,
* пишет персонализированные сообщения,
* фильтрует мусорные вакансии и спам,
* отвечает работодателям осмысленно, а не шаблонами,
* подбирает лучшие вакансии по вашему профилю.

Это не просто автоматизация — это полноценный **AI-помощник**, который сопровождает весь процесс поиска работы.

---

<!-- omit from toc -->
## 🧠 **Возможности**

* **AI-генерация откликов**, сопроводительных писем и ответов работодателям.
* **Автоматический отклик на похожие вакансии** с интеллектуальной проверкой релевантности через LLM.
* **AI-анализ вакансий** — фильтрация плохих, нерелевантных или фейковых предложений.
* **Ответ работодателям в чатах** (включая массовые рассылки).
* **Очистка старых и нерелевантных откликов**.
* **Гибкие фильтры поиска вакансий**: зарплата, опыт, график, ключевые слова, дата публикации, метро и многое другое.
* **Отправка любых запросов в API HeadHunter** — для продвинутых пользователей.
* **Управление токенами и конфигом**.

Если вы хотите получать больше приглашений, экономить время и при этом выглядеть профессионально — этот инструмент создан для вас.

---

<!-- omit from toc -->
## Содержание

- [📦 Установка](#-установка)
    - [1. Вам понадобится:](#1-вам-понадобится)
    - [2. Клонируйте репозиторий:](#2-клонируйте-репозиторий)
    - [3. Установите зависимости:](#3-установите-зависимости)
- [🚀 Запуск](#-запуск)
    - [1. Настройте конфигурацию](#1-настройте-конфигурацию)
    - [2. Пройдите авторизацию в HH.ru](#2-пройдите-авторизацию-в-hhru)
    - [3. Просмотр доступных команд](#3-просмотр-доступных-команд)
- [📘 Общая структура](#-общая-структура)
- [🌍 Глобальные параметры](#-глобальные-параметры)
- [🔐 1. Авторизация](#-1-авторизация)
- [✉️ 2. Автоматический AI-отклик на похожие вакансии](#️-2-автоматический-ai-отклик-на-похожие-вакансии)
    - [`apply-similar`](#apply-similar)
- [💬 3. AI-ответ работодателям](#-3-ai-ответ-работодателям)
- [🧹 4. Очистка старых откликов](#-4-очистка-старых-откликов)
- [📄 5. Управление резюме](#-5-управление-резюме)
- [❓ 6. Кто я?](#-6-кто-я)
- [⚙️ 7. Конфигурация (`config.toml`)](#️-7-конфигурация-configtoml)
  - [🌐 `[proxy]` — настройки прокси](#-proxy--настройки-прокси)
  - [👤 `[candidate]` — профиль кандидата](#-candidate--профиль-кандидата)
- [🤖 AI / LLM-настройки](#-ai--llm-настройки)
  - [📝 `llm.cover_letters` — генерация сопроводительных писем](#-llmcover_letters--генерация-сопроводительных-писем)
    - [Опции модели](#опции-модели)
    - [Промпт](#промпт)
    - [Дополнительные сообщения](#дополнительные-сообщения)
  - [🎯 `llm.verify_relevance` — проверка релевантности вакансий](#-llmverify_relevance--проверка-релевантности-вакансий)
    - [Опции](#опции)
    - [Промпт](#промпт-1)
  - [💬 `llm.chat_reply` — AI-ответы работодателям](#-llmchat_reply--ai-ответы-работодателям)
    - [Опции](#опции-1)
    - [Промпт](#промпт-2)
- [💬 Шаблонные сообщения работодателям, без AI](#-шаблонные-сообщения-работодателям-без-ai)
  - [`default_messages.chat_reply` — Шаблонное сообщение работодателю для массовой рассылки](#default_messageschat_reply--шаблонное-сообщение-работодателю-для-массовой-рассылки)
    - [Опции](#опции-2)
    - [Переменные для шаблона](#переменные-для-шаблона)
  - [`default_messages.cover_letter` — Шаблонное сопроводительное письмо](#default_messagescover_letter--шаблонное-сопроводительное-письмо)
    - [Опции](#опции-3)
    - [Переменные для шаблона](#переменные-для-шаблона-1)
- [📝 Управление шаблонными ответами без AI](#-управление-шаблонными-ответами-без-ai)
- [🧭 Рекомендации](#-рекомендации)
- [❤️ Поддержка](#️-поддержка)
- [Лицензия](#лицензия)

---

# 📦 Установка

### 1. Вам понадобится:

* **Python 3.10+**
* **Poetry**

### 2. Клонируйте репозиторий:

```bash
git clone https://github.com/your-username/headhunter-automation.git
cd headhunter-automation
```

### 3. Установите зависимости:

```bash
poetry install
```

---

# 🚀 Запуск

Перед началом работы выполните несколько простых шагов:

### 1. Настройте конфигурацию
Перейдите в директорию `config/` и переименуйте файл:

```bash
config.toml.example → config.toml
```

Заполните необходимые поля (см. [⚙️ 7. Конфигурация (`config.toml`)](#️-7-конфигурация-configtoml)).

---

### 2. Пройдите авторизацию в HH.ru

Авторизация обязательна для работы всех функций:

```bash
poetry run headhunter-automation authorize
```

После входа приложение автоматически сохранит `access_token` и `refresh_token`.

---

### 3. Просмотр доступных команд

Чтобы увидеть полный список возможностей инструмента:

```bash
poetry run headhunter-automation [ОПЕРАЦИЯ] --help
```

---

# 📘 Общая структура

```
Usage: headhunter-automation [OPERATION] [OPERATION_OPTIONS] [GLOBAL_OPTIONS]
```

---

# 🌍 Глобальные параметры

| Опция                | Описание                 |
| -------------------- | ------------------------ |
| `--config-path PATH` | Путь к конфигу           |
| `--data-path PATH`   | Путь к данным            |
| `--verbosity`        | Уровень логов            |
| `--delay`            | Задержка между запросами |
| `--user-agent`       | Кастомный User-Agent     |
| `--proxy-url`        | Использовать прокси      |

---

# 🔐 1. Авторизация

```bash
poetry run headhunter-automation authorize
```

Сохраняет `access_token` и `refresh_token`.

---

# ✉️ 2. Автоматический AI-отклик на похожие вакансии

### `apply-similar`

ИИ анализирует каждую вакансию, проверяет релевантность, и пишет отклик, основываясь на резюме кандидата и описании вакансии.

```bash
poetry run headhunter-automation apply-similar [PARAM]
```

Ключевые опции:

| Опция                | Описание                                    |
| -------------------- | ------------------------------------------- |
| `--force-message`    | Отправлять сопроводительное письмо на каждую вакансию |
| `--ai`               | Генерация текста отклика через ИИ           |
| `--verify-relevance` | Проверка релевантности вакансии LLM-моделью |

Остальные опции можно увидеть в --help для этой операции

---

# 💬 3. AI-ответ работодателям

Вы можете вести общение с рекрутерами вместе с поддержкой AI.

```bash
poetry run headhunter-automation reply-employers [ОПЦИЯ]
```

В чате вы можете писать сообщения, использовать команды /ai, /ban, /cancel и отправлять ответы работодателям.

Ключевые опции:

| Команда                     | Описание                                    |
| --------------------------- | ------------------------------------------- |
| `/ban`                      | Заблокировать этого работодателя |
| `/cancel`                   | Отменить заявку на эту вакансию           |
| `/ai`                       | Попросить AI сделать ваш ответ более профессиональным или написать его с нуля, если после команды не следует текст. К AI передаются сообщения в чате и ваше сообщение, которое вы хотите отправить |
| `Enter`                     | Пропуск, если после команды не следует текст, или отправка |

Фильтры и настройки можно увидеть в --help для этой операции

---

# 🧹 4. Очистка старых откликов
Вы можете установить фильтры и очистить все ваши отклики
```bash
poetry run headhunter-automation clear-negotiations [ОПЦИЯ]
```

Фильтры и настройки можно увидеть в --help для этой операции

---

# 📄 5. Управление резюме

Обновляет резюме автоматически.

```bash
poetry run headhunter-automation update-resumes
```

---

# ❓ 6. Кто я?

Показать текущего пользователя:

```bash
poetry run headhunter-automation whoami
```

---

# ⚙️ 7. Конфигурация (`config.toml`)

После переименования файла `config.toml.example → config.toml`, вам станет доступна гибкая система настроек для работы с прокси, профилем кандидата и LLM-модулями.

Ниже — полное описание всех параметров.

---

## 🌐 `[proxy]` — настройки прокси

```toml
[proxy]
proxy_url = ""
```

Используется, если вы хотите отправлять запросы к API HH через прокси-сервер.

* `proxy_url` — строка вида:

  * `http://login:pass@host:port`
  * `socks5://host:port`
  * или пустая строка, если прокси не нужен.

---

## 👤 `[candidate]` — профиль кандидата

```toml
[candidate]
info = """CANDIDATE_INFO"""
```

Здесь указывается ваш профиль, который используется LLM для:

* генерации сопроводительных писем,
* ответов работодателям,
* анализа релевантности.

Рекомендуется описать:
*ваш опыт, стек технологий, сильные стороны, желаемую позицию.*

---

# 🤖 AI / LLM-настройки

AI-модуль состоит из трёх независимых подсистем:

1. `cover_letters` — генерация сопроводительных писем
2. `verify_relevance` — определение релевантности вакансии
3. `chat_reply` — ответы работодателям в чатах

Каждый блок имеет собственные параметры `options`, `prompts`, `messages`.

---

## 📝 `llm.cover_letters` — генерация сопроводительных писем

### Опции модели

```toml
[llm.cover_letters.options]
provider       = "groq"
model_name     = "openai/gpt-oss-20b"
temperature    = 0.7
max_tokens     = 1000
top_p          = 0.9
api_key        = "API_KEY"
```

**Описание параметров:**

| Параметр      | Значение                                           |
| ------------- | -------------------------------------------------- |
| `provider`    | Провайдер LLM (Groq / OpenAI / LM Studio / Local). |
| `model_name`  | Имя модели.                                        |
| `temperature` | Креативность (0 — строго, 1 — творчески).          |
| `max_tokens`  | Максимальная длина ответа.                         |
| `top_p`       | Вероятностная фильтрация (рекомендуется 0.7–0.95). |
| `api_key`     | API ключ от LLM-провайдера.                        |

### Промпт

```toml
[llm.cover_letters.prompts]
system = """
Ты — AI-ассистент. Составь полностью готовое сопроводительное письмо на вакансию на русском языке.
Письмо должно быть 3-4 предложения, обычным текстом, лаконичное, вежливое, без Markdown, без скобок и placeholders.
Выдели ключевые навыки кандидата и почему он подходит для вакансии.
Генерируй письмо сразу полностью, без необходимости продолжения.
"""
```

Модель создаёт **полностью готовое письмо**, используется в командах типа:

```
apply-similar --force-message --ai
```

### Дополнительные сообщения

```toml
[llm.cover_letters.messages]
footer_msg = "FOOTER_MSG"
```

`footer_msg` автоматически пишется в конце каждого сопроводительного письма, если не требуется, оставить значение пустым.

---

## 🎯 `llm.verify_relevance` — проверка релевантности вакансий

Используется в `apply-similar` перед откликом.

### Опции

```toml
[llm.verify_relevance.options]
provider       = "groq"
model_name     = "openai/gpt-oss-20b"
temperature    = 0.7
max_tokens     = 1000
top_p          = 0.9
api_key        = "API_KEY"
```

### Промпт

```toml
[llm.verify_relevance.prompts]
system = """
Ты — AI-модель, которая определяет релевантность кандидата вакансии.
Твоя задача: вернуть **строго 1 или 0**.
1 — кандидат потенциально заинтересован в вакансии и его навыки соответствуют требованиям.
0 — кандидат не подходит или его профиль не пересекается с требуемыми технологиями.
Учитывай стек кандидата и стек вакансии. Если в навыках кандидата отсутствуют ключевые технологии вакансии, возвращай 0.
Ответ должен содержать только одну цифру: 1 или 0. Никаких пояснений, текста или символов.
"""
```

Модель возвращает только **одну цифру**.
Никакого текста.

---

## 💬 `llm.chat_reply` — AI-ответы работодателям

Используется в команде `reply-employers`:

### Опции

```toml
[llm.chat_reply.options]
provider       = "groq"
model_name     = "openai/gpt-oss-20b"
temperature    = 0.4
max_tokens     = 500
top_p          = 0.7
api_key        = "API_KEY"
```

### Промпт

```toml
[llm.chat_reply.prompts]
system = """
Ты — интеллектуальный помощник кандидата, который ведёт переписку с работодателем.

Твоя задача — СФОРМИРОВАТЬ один короткий (1–2 предложения), профессиональный и содержательный ответ работодателю,
опираясь на:
1) последние сообщения работодателя ( <- ),
3) ответы кандидата ( -> ),
4) профиль кандидата,
5) возможный краткий ввод пользователя (часто неполный и хаотичный - надо интерпретировать).

Требования:
- Отвечай всегда от лица кандидата.
- Стиль: деловой, чёткий, уверенный, без лишних фраз и без повторов.
- Если есть вопросы работодателя - обязательно дай короткие прямые ответы.
- Любой ввод нормализуй в профессиональный текст.
- Если пользователь ничего не указал - создай краткий ответ сам, но без самопрезентаций и длинных вступлений.
- Сформируй только один лаконичный ответ.

Выводи только финальное сообщение, без комментариев.
Профиль кандидата:
"""
```

Модель формирует только одно финальное сообщение.
Без пояснений и без метаданных.

# 💬 Шаблонные сообщения работодателям, без AI

Если вы хотите отправлять отклики на вакансии или отвечать работодателям без использования AI, вы можете задать шаблонные сообщения в конфиге.

## `default_messages.chat_reply` — Шаблонное сообщение работодателю для массовой рассылки

Используется в команде `reply-employers --reply-message`:

### Опции

```toml
message = """{Здравствуйте|Добрый день}."""
```

Поддерживает рандомизацию шаблона (см. [📝 Управление шаблонными ответами без AI](#-управление-шаблонными-ответами-без-ai))

### Переменные для шаблона

| Переменная      | Описание                         |
|-----------------|----------------------------------|
| `first_name`    | Ваше имя                         |
| `last_name`     | Ваша фамилия                     |
| `email`         | Почта                            |
| `phone`         | Номер телефона                   |
| `vacancy_name`  | Название вакансии                |
| `employer_name` | Название компании работодателя   |

## `default_messages.cover_letter` — Шаблонное сопроводительное письмо

Используется в команде `apply-similar --force-message`:

### Опции

```toml
messages = [
    "{Меня заинтересовала|Мне понравилась} ваша вакансия %(vacancy_name)s",
    "{Прошу рассмотреть|Предлагаю рассмотреть} {мою кандидатуру|мое резюме} на вакансию %(vacancy_name)s",
    ]
```

Поддерживает рандомизацию шаблона (см. [📝 Управление шаблонными ответами без AI](#-управление-шаблонными-ответами-без-ai))

### Переменные для шаблона

| Переменная      | Описание                         |
|-----------------|----------------------------------|
| `first_name`    | Ваше имя                         |
| `last_name`     | Ваша фамилия                     |
| `email`         | Почта                            |
| `phone`         | Номер телефона                   |
| `vacancy_name`  | Название вакансии                |
| `employer_name` | Название компании работодателя   |

# 📝 Управление шаблонными ответами без AI

Шаблонные сообщения пишутся по следующему алгоритму:

```toml
message = "{Первый вариант %(vacancy_name)s |Второй вариант %(vacancy_name)s}"
```

Приложение случайным образом выбирает один из вариантов и подставляет нужные значения в переменные, прописанные в шаблоне (например `%(vacancy_name)s`).
Имена переменных указаны в описании конфиг файла.

# 🧭 Рекомендации

* Ставьте задержку `--delay 0.5` для стабильной работы.
* Используйте AI-режимы — это увеличивает шанс получить отклик.
* Не отправляйте отклики слишком часто — HH может ограничить активность.

---

# ❤️ Поддержка

Нашли ошибку? Хотите улучшить проект?
Открывайте Issue — буду рад помощи!

---

# Лицензия

Проект распространяется под лицензией MIT, подробнее смотреть в [LICENSE](LICENSE).
